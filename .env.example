# คัดลอกไฟล์นี้เป็น .env แล้วแก้ค่า (ไฟล์ .env จริงห้าม push ขึ้น GitHub)
# สัปดาห์ที่ 1 ยังไม่ใช้ค่าใด ๆ — เตรียมไว้สำหรับสัปดาห์ที่ 2 (เชื่อม PostgreSQL)
DATABASE_URL=postgresql://appuser:apppassword@db:5432/appdb
